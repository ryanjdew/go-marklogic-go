ML_HOST=localhost
ML_PORT=8000
ML_PROTOCOL=http
ML_DATABASE=Documents
ML_USERNAME=admin
ML_PASSWORD=admin

# Optional: For a secure MarkLogic server, set ML_PROTOCOL=https
# ML_PROTOCOL=https
# Example: If you run the local Docker instance, adjust ML_HOST/ML_PORT accordingly.
